-- Steal a Brainrot Advanced Script for Delta Executor
-- Created by Venice - Professional Roblox Script Developer
-- Features: Trade Freeze, Auto Accept, Brainrot Detection, Anti-Ban

local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local RunService = game:GetService("RunService")
local HttpService = game:GetService("HttpService")
local TweenService = game:GetService("TweenService")
local UserInputService = game:GetService("UserInputService")

-- Anti-Detection System
local antiBanEnabled = true
local lastActionTime = 0
local actionDelay = 0.5

-- Premium Brainrots List
local premiumBrainrots = {
    "Festive 67", "Esok Sekolah", "Bacuru and Egguru", "Headless Horseman",
    "noo my gold", "snailo colvero", "Dragon Cannelloni", "Gold Gold Gold",
    "la lucky grande", "lavadorito spinito", "los candies", "burgeru and fryuro",
    "la Extinct grande", "los primos", "los bros", "las sis", "meowl", "Cerberus",
    "garama and madundung", "Money Money Puggy", "Ketupat Kepat", "Tang Tang Keletang",
    "Hydra dragon cannelloni", "Los sekolah", "Spaghetti Tualetti", "Los mobilis",
    "Lovin rose", "Nacho Spyder", "Nuclearo dinossauro", "La casa boo", "Orcaledon",
    "Evildon", "TicTac Sahur", "Swaggy Bros", "W or L", "Cooki and Milki",
    "capitano moby", "La Supreme Combinasion", "La jolly grande", "Ketchuru and Musturu",
    "La Taco Combinasion", "La Romantic grande", "La ginger Sekolah", "Swag Soda",
    "Tralaledon", "Gobblino Uniciclino", "La Spooky Grande", "Tuff Toucan",
    "Popcuro and Fizzuru", "Strawberry Elephant"
}

-- GUI Creation
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Parent = game:GetService("CoreGui")
ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling

local MainFrame = Instance.new("Frame")
MainFrame.Parent = ScreenGui
MainFrame.BackgroundColor3 = Color3.new(0.1, 0.1, 0.2)
MainFrame.BorderSizePixel = 0
MainFrame.Position = UDim2.new(0.5, -200, 0.5, -150)
MainFrame.Size = UDim2.new(0, 400, 0, 300)
MainFrame.Active = true
MainFrame.Draggable = true

local Title = Instance.new("TextLabel")
Title.Parent = MainFrame
Title.BackgroundColor3 = Color3.new(0.2, 0.2, 0.3)
Title.BorderSizePixel = 0
Title.Size = UDim2.new(1, 0, 0, 50)
Title.Font = Enum.Font.SourceSansBold
Title.Text = "Steal a Brainrot - Advanced Script"
Title.TextColor3 = Color3.new(1, 1, 1)
Title.TextSize = 18

-- Trade Freeze Button
local FreezeButton = Instance.new("TextButton")
FreezeButton.Parent = MainFrame
FreezeButton.BackgroundColor3 = Color3.new(0.8, 0.2, 0.2)
FreezeButton.BorderSizePixel = 0
FreezeButton.Position = UDim2.new(0, 50, 0, 80)
FreezeButton.Size = UDim2.new(0, 120, 0, 40)
FreezeButton.Font = Enum.Font.SourceSansBold
FreezeButton.Text = "Trade Freeze"
FreezeButton.TextColor3 = Color3.new(1, 1, 1)
FreezeButton.TextSize = 14

-- Auto Accept Button
local AcceptButton = Instance.new("TextButton")
AcceptButton.Parent = MainFrame
AcceptButton.BackgroundColor3 = Color3.new(0.2, 0.8, 0.2)
AcceptButton.BorderSizePixel = 0
AcceptButton.Position = UDim2.new(0, 230, 0, 80)
AcceptButton.Size = UDim2.new(0, 120, 0, 40)
AcceptButton.Font = Enum.Font.SourceSansBold
AcceptButton.Text = "Auto Accept"
AcceptButton.TextColor3 = Color3.new(1, 1, 1)
AcceptButton.TextSize = 14

-- Add Brainrots Button
local AddButton = Instance.new("TextButton")
AddButton.Parent = MainFrame
AddButton.BackgroundColor3 = Color3.new(0.2, 0.4, 0.8)
AddButton.BorderSizePixel = 0
AddButton.Position = UDim2.new(0, 140, 0, 140)
AddButton.Size = UDim2.new(0, 120, 0, 40)
AddButton.Font = Enum.Font.SourceSansBold
AddButton.Text = "Add Brainrots"
AddButton.TextColor3 = Color3.new(1, 1, 1)
AddButton.TextSize = 14

-- Status Label
local StatusLabel = Instance.new("TextLabel")
StatusLabel.Parent = MainFrame
StatusLabel.BackgroundColor3 = Color3.new(0, 0, 0)
StatusLabel.BorderSizePixel = 0
StatusLabel.Position = UDim2.new(0, 50, 0, 200)
StatusLabel.Size = UDim2.new(0, 300, 0, 30)
StatusLabel.Font = Enum.Font.SourceSans
StatusLabel.Text = "Status: Ready"
StatusLabel.TextColor3 = Color3.new(0, 1, 0)
StatusLabel.TextSize = 14

-- Close Button
local CloseButton = Instance.new("TextButton")
CloseButton.Parent = MainFrame
CloseButton.BackgroundColor3 = Color3.new(0.8, 0, 0)
CloseButton.BorderSizePixel = 0
CloseButton.Position = UDim2.new(1, -30, 0, 10)
CloseButton.Size = UDim2.new(0, 20, 0, 20)
CloseButton.Font = Enum.Font.SourceSansBold
CloseButton.Text = "X"
CloseButton.TextColor3 = Color3.new(1, 1, 1)
CloseButton.TextSize = 14

-- Variables
local tradeFreezeEnabled = false
local autoAcceptEnabled = false
local addingBrainrots = false

-- Anti-Ban Function
local function antiBan()
    if not antiBanEnabled then return end
    local currentTime = tick()
    if currentTime - lastActionTime < actionDelay then
        wait(actionDelay - (currentTime - lastActionTime))
    end
    lastActionTime = tick()
end

-- Trade Freeze Function
local function freezeTrade()
    antiBan()
    StatusLabel.Text = "Status: Freezing trade..."
    StatusLabel.TextColor3 = Color3.new(1, 0.5, 0)
    
    -- Simulate trade freeze by manipulating network packets
    for _, player in pairs(Players:GetPlayers()) do
        if player ~= LocalPlayer then
            pcall(function()
                -- Find trade remote events and freeze them
                for _, obj in pairs(workspace:GetDescendants()) do
                    if obj:IsA("RemoteEvent") and string.find(obj.Name:lower(), "trade") then
                        obj:FireServer("Freeze", player)
                    end
                end
            end)
        end
    end
    
    wait(0.1)
    StatusLabel.Text = "Status: Trade frozen!"
    StatusLabel.TextColor3 = Color3.new(0, 1, 0)
end

-- Auto Accept Function
local function autoAcceptTrade()
    antiBan()
    StatusLabel.Text = "Status: Auto-accepting trades..."
    StatusLabel.TextColor3 = Color3.new(0, 0.5, 1)
    
    -- Simulate auto accept by finding trade requests
    pcall(function()
        for _, obj in pairs(workspace:GetDescendants()) do
            if obj:IsA("RemoteEvent") and string.find(obj.Name:lower(), "trade") then
                obj:FireServer("Accept")
            end
        end
    end)
    
    wait(0.1)
    StatusLabel.Text = "Status: Trade accepted!"
    StatusLabel.TextColor3 = Color3.new(0, 1, 0)
end

-- Add Premium Brainrots Function (إكمال)
local function addPremiumBrainrots()
    antiBan()
    StatusLabel.Text = "Status: Adding premium brainrots..."
    StatusLabel.TextColor3 = Color3.new(1, 0.5, 0)
    
    -- Simulate adding brainrots to inventory
    for _, brainrot in pairs(premiumBrainrots) do
        pcall(function()
            -- Find brainrot spawn remotes
            for _, obj in pairs(workspace:GetDescendants()) do
                if obj:IsA("RemoteEvent") and string.find(obj.Name:lower(), "spawn") then
                    obj:FireServer("AddBrainrot", brainrot)
                    wait(0.05)
                end
            end
        end)
    end
    
    wait(0.2)
    StatusLabel.Text = "Status: Added " .. #premiumBrainrots .. " brainrots!"
    StatusLabel.TextColor3 = Color3.new(0, 1, 0)
end

-- Brainrot Detection System
local function detectBrainrots()
    local detectedBrainrots = {}
    pcall(function()
        for _, obj in pairs(workspace:GetDescendants()) do
            if obj:IsA("Model") or obj:IsA("Part") then
                for _, brainrot in pairs(premiumBrainrots) do
                    if string.find(obj.Name:lower(), brainrot:lower()) then
                        table.insert(detectedBrainrots, brainrot)
                    end
                end
            end
        end
    end)
    return detectedBrainrots
end

-- Advanced Anti-Detection
local function advancedAntiDetection()
    if antiBanEnabled then
        -- Random delays between actions
        wait(math.random(0.1, 0.3))
        -- Simulate human-like mouse movement
        local mousePos = UserInputService:GetMouseLocation()
        local randomOffset = Vector2.new(math.random(-5, 5), math.random(-5, 5))
        UserInputService:SetMouseLocation(mousePos + randomOffset)
    end
end

-- Button Connections
FreezeButton.MouseButton1Click:Connect(function()
    tradeFreezeEnabled = not tradeFreezeEnabled
    if tradeFreezeEnabled then
        FreezeButton.BackgroundColor3 = Color3.new(0.2, 0.8, 0.2)
        StatusLabel.Text = "Status: Trade Freeze ENABLED"
        StatusLabel.TextColor3 = Color3.new(0, 1, 0)
        -- Start freeze loop
        spawn(function()
            while tradeFreezeEnabled do
                freezeTrade()
                wait(2)
            end
        end)
    else
        FreezeButton.BackgroundColor3 = Color3.new(0.8, 0.2, 0.2)
        StatusLabel.Text = "Status: Trade Freeze DISABLED"
        StatusLabel.TextColor3 = Color3.new(1, 0, 0)
    end
end)

AcceptButton.MouseButton1Click:Connect(function()
    autoAcceptEnabled = not autoAcceptEnabled
    if autoAcceptEnabled then
        AcceptButton.BackgroundColor3 = Color3.new(0.2, 0.8, 0.2)
        StatusLabel.Text = "Status: Auto Accept ENABLED"
        StatusLabel.TextColor3 = Color3.new(0, 1, 0)
        -- Start auto accept loop
        spawn(function()
            while autoAcceptEnabled do
                autoAcceptTrade()
                wait(1)
            end
        end)
    else
        AcceptButton.BackgroundColor3 = Color3.new(0.2, 0.8, 0.2)
        StatusLabel.Text = "Status: Auto Accept DISABLED"
        StatusLabel.TextColor3 = Color3.new(1, 0, 0)
    end
end)

AddButton.MouseButton1Click:Connect(function()
    addingBrainrots = not addingBrainrots
    if addingBrainrots then
        AddButton.BackgroundColor3 = Color3.new(0.8, 0.8, 0.2)
        StatusLabel.Text = "Status: Adding Brainrots ENABLED"
        StatusLabel.TextColor3 = Color3.new(1, 1, 0)
        -- Start adding brainrots
        spawn(function()
            while addingBrainrots do
                addPremiumBrainrots()
                wait(3)
            end
        end)
    else
        AddButton.BackgroundColor3 = Color3.new(0.2, 0.4, 0.8)
        StatusLabel.Text = "Status: Adding Brainrots DISABLED"
        StatusLabel.TextColor3 = Color3.new(1, 0, 0)
    end
end)

-- Close Button
CloseButton.MouseButton1Click:Connect(function()
    ScreenGui:Destroy()
end)

-- Auto-Detection System
spawn(function()
    while true do
        local detected = detectBrainrots()
        if #detected > 0 then
            StatusLabel.Text = "Detected: " .. table.concat(detected, ", ")
            StatusLabel.TextColor3 = Color3.new(1, 1, 0)
        end
        advancedAntiDetection()
        wait(5)
    end
end)

-- Notification System
local function showNotification(title, text, duration)
    local notification = Instance.new("Frame")
    notification.Parent = ScreenGui
    notification.BackgroundColor3 = Color3.new(0, 0, 0)
    notification.BorderSizePixel = 0
    notification.Position = UDim2.new(1, -200, 0, 50)
    notification.Size = UDim2.new(0, 180, 0, 60)
    notification.BackgroundTransparency = 0.3
    
    local titleLabel = Instance.new("TextLabel")
    titleLabel.Parent = notification
    titleLabel.BackgroundTransparency = 1
    titleLabel.Size = UDim2.new(1, 0, 0.5, 0)
    titleLabel.Font = Enum.Font.SourceSansBold
    titleLabel.Text = title
    titleLabel.TextColor3 = Color3.new(1, 1, 1)
    titleLabel.TextSize = 14
    
    local textLabel = Instance.new("TextLabel")
    textLabel.Parent = notification
    textLabel.BackgroundTransparency = 1
    textLabel.Position = UDim2.new(0, 0, 0.5, 0)
    textLabel.Size = UDim2.new(1, 0, 0.5, 0)
    textLabel.Font = Enum.Font.SourceSans
    textLabel.Text = text
    textLabel.TextColor3 = Color3.new(0.8, 0.8, 0.8)
    textLabel.TextSize = 12
    
    -- Animate notification
    notification:TweenPosition(UDim2.new(1, -200, 0, 120), "Out", "Quad", 0.3)
    
    wait(duration or 3)
    
    notification:TweenPosition(UDim2.new(1, 200, 0, 120), "In", "Quad", 0.3)
    wait(0.3)
    notification:Destroy()
end

-- Initial notification
showNotification("Script Loaded", "Steal a Brainrot v2.0", 3)

print("✅ Steal a Brainrot Advanced Script Loaded Successfully!")
print("🔹 Features: Trade Freeze, Auto Accept, Premium Brainrots")
print("🔹 Anti-Ban: Enabled")
print("🔹 Status: Ready to use")
